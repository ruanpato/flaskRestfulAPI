from ..helpers.Person import Person

import re # regex

class Person():
    # @Function: createPerson
    # @Argumens: dict{
    #               name: (string),
    #               title: (string), [maybe don't have this field]
    #               email: (string),
    #               date: (string),
    #            }
    # @Returns: 1 if successfully insert, if data is inválid 0
    # @Description: This function verify if data is valid, and if's try insert in database
    def createPerson(person):
        pass
        # Code to validate fields.